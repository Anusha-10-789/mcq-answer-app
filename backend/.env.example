CORS_ORIGINS=http://localhost:5173

# Required. Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional. If set, /answer and /download require this exact value in an
# "X-App-Password" header. Leave blank for open local development. Set this
# when deploying publicly so random visitors can't use your Gemini quota.
APP_PASSWORD=
